<div class="container">
    <div class="row" style="margin-top:50px">
        <div class="col-sm-6">
            <form>
                <div class="form-group">
                    {{input type="text" class="form-control" value=title placeholder="Add a New Task"}}
                </div>
                <button {{action 'addTodo'}} class="btn btn-primary">Submit</button>
            </form>
        </div>
    </div>
</div>
